import { IBM_Plex_Sans, Inter, Poppins, Rubik } from '@next/font/google';

export const title = Poppins({
    subsets: ['latin'],
    weight: ['400', '500', '600', '700'],
});
export const text = Inter({
    subsets: ['latin'],
});
